/*
 * app.js testing specs
 */
describe("feedly app initialises", function () {
  it("angular module", function () {
      
      expect(feedly).not.toBeNull();
      //expect(klaud).toBe(object);
  });
});